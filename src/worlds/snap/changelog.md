# Snap! — changelog

Newest entries first. The 2026-09-01 → 2026-09-03 history of this build (the rethink, the ten rolls, the Design prompt, steps 1–3 and every read of James's) is in `src/worlds/the-valence-lab/changelog.md`; entries from the move on are here.

## 2026-09-05 — Claude (Fable 5.1) — Scope controls centred, bigger

James: recentre the controls; disclaimer to the left, midway up; larger labels; bigger slider targets. The dials plate sits bottom-centre; the READING plate is left-middle; labels 11 → 14px in ink; sliders 170px wide with a 22px amber thumb on a 6px track and a 32px hit height; close 13px.

Then: the READING plate pinned to the scope, not the window — its right edge 20px off the rim.

## 2026-09-05 — Claude (Fable 5.1) — The scope gets its body

James: give it back some physicality — a bevelled metal-and-plastic rim around the viewport, the controls in a connected panel, a name card, a plate for the disclaimer, a slight rectangular outer frame; an overlay to the whole window, not a circle on an arm. Built in CSS only: `.lens-wrap` is the frame (22px in from the window, a double hairline, faint gradient, inset bevel); `.rim` is the ring (a conic brushed-metal gradient with a plastic collar in the box-shadow stack, inset highlight above and shadow below, a ring of fine ticks every 6°); `.lens` sits inside it with a dark inner lip; three `.plate`s share one bevelled style — SPECIMEN (name + formula, top-left), READING (the disclaimer, bottom-left), and the dials with the close button (bottom-left, under it). Verified at 1920×1080.

## 2026-09-05 — Claude (Fable 5.1) — The lens, twice the size

James: "make the scope like twice this size. When I zoom in, I want to be able to see a lot of the molecule." The lens now fills the window (92% of the shorter side, up to 1240px); the name sits top-left, the caption and the three dials bottom-left, all over the veil instead of stacked under the lens. The wheel zooms in closer (near limit 6 → 2.5).

## 2026-09-05 — Claude (Fable 5.1) — Highlights leave with their atoms

James: highlighted atoms that get cleared kept their rings. The selection set held atoms the bench no longer had (clear, guided-mode chapter starts and free mode replace the atom list outright). `drawSelection` now prunes the selection to atoms still on the bench every frame.

## 2026-09-05 — Claude (Fable 5.1) — The Scope on the bench; the Valence Lab retired

James's five calls (both placements; 3-D lens; three sliders plus mouse rotate and wheel zoom; all eighteen atoms; "buh bye" to the lab). Built:

`scope.js` (an ES module, loaded the first time it is asked for — served only). Click an atom or a molecule, then SCOPE in the rail (or "scope" in the right-click menu). A veil drops over the bench and a round lens opens with the reading in 3-D: the swarm (7,000 dots, each a genuine sample — hydrogen-like |ψ|² with Slater screening for a lone atom via `orbitals.js`, the Metropolis walk over the baked Hartree-Fock density for a molecule via `density.js`), the shells (the ρ = 0.004 isosurface of that density for molecules; s spheres and p dumbbells of the valence shell for atoms), and the nucleus (protons and neutrons, instanced, jittering, magnified). Dots are coloured by subshell for atoms (inner shells dimmed) and by element for molecules. It turns slowly on its own until you drag it; the wheel zooms. Three dials under the lens — cloud, shells, nucleus — remembered in localStorage (`snap-scope`). Escape, the close button, or a click on the veil closes it. The physics modules are the lab's own, copied into this folder; `orbitals.js` gained the twelve missing elements so all eighteen bench atoms read (copper refuses: "The scope reads the first eighteen elements"); molecules beyond the nine baked refuse by name. Framing: atoms by the visible edge of their valence shell (sodium's far 3s tail no longer pushes the camera out), molecules by their extent; dot size holds on screen at any distance.

Verified in the pane at 1920×1080: water (dots, shell, three nuclei), sodium (bright core, sparse 3s halo, 1s² 2s² 2p⁶ 3s¹ in the head), methane (the tetrahedral shell), the copper refusal, Escape closes. Console clean.

THE VALENCE LAB IS ARCHIVED (his "buh bye"): `archive/the-valence-lab/` via the admin archive route; its row left the admin panel; the registry regeneration leaked the drafts (Lumina, Snap) into the live pool as the known gotcha says and was restored from git. Snap holds its own copies of `orbitals.js`, `density.js` and `assets/molecules-data.js`; the Hartree-Fock solver and the sims stay in `tmp/the-valence-lab/` (the sims still import from the old world path — repoint them when the solver batch runs). The lab's changelog, with Snap's 2026-09-01→03 history, is in the archive.

AWAITING JAMES'S EYES on the scope.

## 2026-09-04 — Claude (Fable 5.1) — Step 10.5: the hold view is the real electron cloud

Same night, his "engage!". Holding an atom or molecule now dims the bench under a veil (the background at 88%) and shows the electron cloud in place of the dashed-shell X-ray:

THE REAL DENSITY (`cloud.js`, new). For the nine molecules the Valence Lab baked (H₂, N₂, O₂, H₂O, CO₂, CH₄, NH₃, H₂O₂, C₂H₄ — `assets/molecules-data.js`, copied from the lab, RHF/UHF STO-3G, the same converged density matrices), the cloud is the genuine Hartree-Fock electron density: evaluated on a 96×96 grid in the molecule's own plane (the direction its atoms spread least, found over a fan of 400 directions), integrated through ±3 bohr, eased to nothing over the last 1.2 bohr before the 4.6-bohr cutoff so the grid never shows a rim. The column density is kept raw and coloured on demand with a LOG ramp — t = ln(1 + c/0.02)/ln(1 + 40/0.02), alpha = t^0.55, blue (110,175,255) running to white above t = 0.72 — then painted twice with added light: a blurred halo (20 px, 65%) under the sharp copy. That is the layered glow James liked in the mock, with the real shape under it. The image is FITTED onto the atoms you are holding: the best similarity transform (rotation, scale, reflection) over every symbol-consistent pairing of bench atoms to baked atoms; the nucleus marks (+8, +1…) sit at the fitted true positions. Build costs 22–74 ms per molecule; all nine build in idle time after load and cache in localStorage as PNGs (`snap-cloud-v3`), so a hold never waits.

THE FALLBACK for anything not baked (single atoms, other molecules): the mock's layered glow itself — five soft discs per atom, white at the centre, hydrogens tucked toward their partner — with the same nucleus marks. Under file:// the import fails quietly and every hold uses the fallback. The benzene ring glow stays. Names, ledgers and the configuration line stay on top of the veil.

Judged in `tmp/snap/hold-lab.html` (KEEP — the nine clouds side by side at bench scale with live dials for the ramp and halo; it shares `SnapCloud.paint` with the world). Rounds: the first ramp blew every core out white to ring size; a linear ramp made flat blue discs; the log ramp gave wings and cores; the halo brought the glow; the soft cutoff removed the rounded-square rim. Verified in the world at 1920×1080: water and ethene held show fitted clouds; console clean. AWAITING JAMES'S EYES.

## 2026-09-04 — Claude (Fable 5.1) — Bench editing

James's "engage!" on the four-point plan. Built:

SELECTION. Drag on empty bench draws an amber dashed box; release selects every atom whose centre is inside (Shift keeps the old selection too). Click an atom selects it (and opens its card as before); Shift-click adds or removes one and does nothing else; a plain click on empty bench clears. Selected atoms wear a slow-marching amber dashed ring (`drawSelection` in engine.js, under the same camera transform). Panning the camera at zoom moved to Shift-drag or middle-drag on empty bench.

KEYS. Delete / Backspace removes the selection (bonds unlinked, X-ray and drag state cleared — `E.removeAtoms`). Ctrl-C copies the selection as a snapshot: positions about the centroid, charges, and every bond among the copied atoms (`E.snapshot`). Ctrl-V rebuilds it at the mouse (`E.restore`: covalent bonds re-linked at their order, metallic re-linked, ionic pairs hand their electron over again, lone ions keep their charge, tethers last), somewhere clear — the asked-for spot, else the nearest of a widening ring of spots that keeps 50px from everything and stays under the guide strip. Ctrl-D duplicates beside the original (clipboard untouched). Ctrl-A selects all. Escape clears the selection after it has closed any menu, card or panel. Pasting counts as landing (the invitation goes; the 140-atom cap holds).

THE MENU. Right-click anywhere on the bench opens a small menu in the panel chrome (mono caps rows, `#menu`, position: fixed, kept inside the window). On an atom: the ion line first — "lose an electron" / "gain an electron" / "back to neutral" by what that atom would do — then copy / duplicate / delete for that atom, or for the whole selection when you right-clicked inside it ("copy 6 atoms"). On empty bench: paste (dim with nothing copied), select all, clear the bench. Dismiss: click away, Escape, wheel, window blur. Right-click no longer ionizes directly; chapter 7's four steps now say "right-click the sodium and choose lose an electron" and so on (their waits are unchanged).

Verified in the pane at 1920×1080: box-select over a landed hydrogen → Ctrl-C → Ctrl-V lands a ringed copy clear of the original → right-click shows the six rows → Escape closes it → Delete removes the copy. Bond harness ALL GREEN. Tags: css 44, engine 21, app 48, guide 17.

HIS FIRST TRY: box, click, Shift-click, Delete, Escape worked; Ctrl-V and the right-click menu did nothing, Ctrl-D bookmarked the page. Cause: a later edit (the cloud warm-up line) was spliced in front of the `let mouse, mouseIn, clip` declaration and its trailing comment swallowed it, so every path touching the cursor position or the clipboard threw. The pane had passed because its test dispatched events before that edit. Declaration restored on its own line; verified with real right-click and a dispatched Ctrl-D (2 atoms → 4). Tag app 50.

## 2026-09-04 — Claude (Fable 5.1) — Bonds by hand: single first, push for more, and rings that close

James, on chapter 9: six carbons chained up stiff with doubles and would not bend into a ring. His nod on the plan (single first; push again for double, triple; chapter text; benzene hands-on). What it took, all in `engine.js`, each found by the new harness `tmp/snap/bond-sim.mjs` (KEEP — 36 assertions, seeded, `SNAPDBG=1` prints slot geometry):

1. **First contact is a single bond** (`link` order 1). **Drag one atom onto its bonded partner and they share another pair** — each step is its own push: the cursor has to leave the partner (1.4 R) and come back inside 0.9 R, having actually moved; a partner drifting under a still cursor does not count; 0.6 s age gate. `upgradeEvent` flashes, chords and whispers ("A second pair shared: a double bond… it cannot twist"). The single-bond whisper now says how to get the second pair when both still have a place open.
2. **Viscous hold while dragging** (`K_HOLD` 6 per bond of distance, capped at 6): the far end of a molecule stays put, so a chain bends instead of sliding as one stiff body.
3. **Slots re-pick every 0.2 s for every atom** (`reslot`; hand phases carry over). Chain atoms with two or three heavy neighbours use 120° zigzag patterns (`CHAIN2`/`CHAIN3`), and heavy neighbours take the wide slots **in counter-clockwise order** — the mirror assignment fit with equal and opposite errors, the torque cancelled, and the atom sat stable with its hydrogen pointing into the ring.
4. **No three- or four-membered rings**: atoms fewer than five bonds apart along one molecule neither attract nor bond (`pathLen`). Atoms within three bonds may pass through one another (a hydrogen has to be able to swing to the other side of its carbon; a bond rotates in 3-D).
5. **Rings are first-class** (`findRings` on every link/unlink, five- and six-cycles): a shape spring (`K_RING` 1500, torque-cancelled with the angle springs) pulls each ring toward a regular convex polygon in bond order. An angle spring more than 90° off its slot pushes full-strength the short way round (the sideways projection dies at 180°).

Harness: six seeds close the ring by a human-style sweep and settle a perfect hexagon (all six angles 120°, every hydrogen outward), three pushes make benzene; water, methane, ammonia, nitrogen, carbon dioxide, ethene, propane still build; propane stays a zigzag. Guide: chapter 5's oxygen/nitrogen steps, chapter 9's ethene step and a NEW hands-on benzene step (`made:C6H6`, twelve atoms handed) say the push in plain words; the Molecules panel landing lines for O₂, CO₂, N₂, ethene, acetylene, formaldehyde, benzene too. Not eye-verified in-world (the pane is 800 px); loads clean. Tags: engine 19, guide 16, molecules 2.

AWAITING JAMES: the push gesture's feel, a ring closed by hand, the hexagon.

Same night, his first read: a third carbon attached to a C–C arrived as a double. Cause: the first approach itself armed the step (the cursor is beyond 1.4 R at the moment of contact), and now that the chain holds still the shove carries on over the partner. The arming is a REVERSAL now: the cursor has to back off half a radius from its closest approach since the bond formed (or last stepped up), then come inside 0.9 R. A probe of straight-in drags at two speeds, stopping anywhere from the rim to 40 px past the partner's centre, never makes a double; back off and push does. Harness push gesture updated; ALL GREEN over four seeds. Tag engine 20. HIS VERDICT: "It worked. I made a carbon ring. And then I made cyclohexane."

## 2026-09-04 — Claude (Fable 5.1) — The strip is a wall

James at chapter 8 step 2: one or two hydrogens ended up behind the guide strip where nothing can be clicked, and he had to vacuum them out with the carbon from below ("none of the example atoms should accidentally get stuck behind the area where you can't click"). Cause: the bench's top wall was the canvas edge, so anything the physics nudged upward parked under the text. Now `sc.topY` is set every frame from the strip's bottom edge (+8px) while the strip is on and not minimized, the engine's top wall sits there, and the landing trials treat it as the top of the bench. Verified in the pane at 1920×1080 on #ch8-2: every handed atom below the text. Tags: engine 18, app 47.

## 2026-09-04 — Claude (Fable 5.1) — Ion ledger; the plan trimmed

James's morning-after reads. The ion ledger no longer counts an empty shell: a stripped ion reads `SHELLS 1–2 FULL · NO OUTER ELECTRONS / CHARGE +1` (helium-row ions `SHELL 1 FULL · NO OUTER ELECTRONS`; bare H⁺ `NO ELECTRONS / CHARGE +1`); the sum line beneath (`11 P⁺ + 10 E⁻ = +1`) is what explains the plus one. Filled shells unchanged. Tag engine 17.

The plan (reimagine.md §13): step 9 crossed off ("we've already done this" — the Design look has been the world's since step 1), step 10 rewritten to one plain sentence (exits, registry, changelog, check-worlds, he removes "unwired"), step 11 removed ("tune by eye is not a step, it's just how we do this"). Tonight, his word: bench editing and the hold view (10.5).

## 2026-09-04 — Claude (Fable 5.1) — Step 6: chapters 7–12, right-click ions, and copper

James's go ("copper real block… ions: let's go with your idea… be creative!"). Two new engine behaviours and six chapters:

RIGHT-CLICK IONIZE (`ionize(sc,a)`, wired on the bench's contextmenu; the right button never starts a drag or X-ray): an atom on its own loses an electron (givers, hydrogen, copper, carbon) or gains one (nitrogen through chlorine), the balance line tips, a warm or cool ring flashes out, a gliss plays, and a plain line says what happened ("Sodium lost an electron: 11 protons, 10 electrons, charge +1. It is an ion now, Na⁺."; hydrogen: "What is left is a bare proton, H⁺, and that is what an acid hands out."). Right-click again = neutral. Bonded atoms refuse ("pull it free first"); full shells refuse ("holds its electrons harder than anything"). Ions attract by charge and tether, and a tethered Na⁺/Cl⁻ pair is recognized as salt.

COPPER, THE FIRST METAL (`Cu` in ELEM, kind `metal`; KEYORD gained Cu; the copper tile unlocks): no hands, no lone pairs, no slots. Copper bonds only to copper, up to six neighbours each (`METAL_MAX`), a `metallic` bond with no lens: a faint bridge and two electrons drifting along it in opposite directions, never settling — the electron sea. A lone copper shows its one loose electron wandering the ring. Six pack into the hexagonal block on their own. Metallic bonds break under heat like singles (melting). `sc.onMetal(n)` reports the block size; a whisper on the first block of three.

THE CHAPTERS (guide.js, waits `ion:SYM` and `metal:N` added): 7 Ions on their own (Na⁺, Cl⁻, the tethered pair as salt, H⁺ = acid), 8 Carbon the builder (methane, ethane, propane — hydrogens first so each carbon keeps its places, then carbon to carbon), 9 Rings and double bonds (ethene by the two-hydrogens-each rule; benzene as a drawing and text — the min-free-hands bond rule makes a hand-built ring a puzzle, so it is not asked for), 10 Alcohol and sugar (methanol hands-on; glucose as a drawing), 11 Chains of life (ammonia hands-on; glycine's atoms handed with a drawing, no wait — "if you want to try it"; the peptide in text), 12 Metals (six coppers → the block, the sea, melting, and the closing line for all twelve). Every target has its drawing in the strip. The finish screen now says all twelve are done.

Verified in the pane at a 1920×1080 bench, engine stepped by hand: chapter 7 all four waits (Na⁺, Cl⁻, tether → salt, bare H⁺ with the small ring), chapter 8 methane, chapter 9 ethene lands CC2 by the rule, chapter 12 six coppers → 9 metallic bonds and the wake; console clean. Ethylene/ethene naming aligned. Tags: app 46, engine 15, guide 15.

AWAITING JAMES'S READ of all six, the right-click feel, and the copper block's look.

OPEN at wrap (his question, Claude's proposal, no go yet): the ledger for an ion reads "SHELL 3: 0 OF 8", which is bookkeeping about places, not something present — an empty shell has no size or charge (Na⁺ is smaller than Na). Proposed: "SHELLS 1–2 FULL · NO OUTER ELECTRONS / CHARGE +1" for a stripped outer shell; a filled one ("SHELL 3: 8 OF 8") stays as is.

## 2026-09-04 — Claude (Fable 5.1) — Collection and Map tabs dropped

James: "let's lose both collection and map. I don't think either of those have much use." The two placeholder rail tabs are gone (the contents panel does the map's job). His idea to keep for later, NOT built yet: the Molecules panel marks the ones you have already made "with a little trophy or something." The position chip now hides outside guided mode (`.pos[hidden]` — the chip's display rule had been beating the attribute).

## 2026-09-04 — Claude (Fable 5.1) — James reads the chapters: the nucleus step, the sum line, small fixes

James walking the chapters and calling changes: chapter 3 gained a NEW STEP 2 on the nucleus (his ask: hide the symbol, call attention to what is going on in there): "Every one of these atoms is neutral… Each proton carries one positive charge, each electron one negative, and they cancel exactly… When an atom gains or loses an electron the balance tips, and a charged atom pulls on anything that can set it right again." On that step (`nucleus:true` → `sc.nucleusMode`): "3 protons" in the serif and the warm ink above each atom with an arrow down into the nucleus, the symbol gone and the PROTONS THEMSELVES as tiny warm dots in a spiral (Z of them), the ledger lines spread lower, and the balance line 1.7× and at full strength. The balance line everywhere now reads as a sum, `3 P⁺ + 3 E⁻ = 0`, tight (his: the plus and equals are "what's actually happening"; the wide gaps were wrong). His framing to keep: these levels are BESPOKE — tuned by hand over time, not fitted to a design system. Contents panel: check marks instead of dots (lifted 1.5px onto the digits' middle), tighter "1 of 3", narrower column. Free mode now always clears the bench. Two refresh bugs: a link/refresh into a step ran before the bench was measured, so every atom landed at the origin (his "clump in the top left, different every time") — the entry now waits for `sc.W>0 && sc.camx>0`. Tags: css 42, app 44, engine 14, guide 14.

## 2026-09-04 — Claude (Fable 5.1) — Guided / Free toggle + the contents panel (step 7, early)

James, flying chapter 3: pulling a hydrogen out mid-chapter left him neither clearly guided nor clearly free, with no way to either — "there needs to be a real clear toggle." And the where-you-are should open a table of contents that goes back to anything already seen, and eventually to any of "64 different levels" ("oh, I really want to go look at the beryllium thing again"). Built:

THE TOGGLE in the rail (`.modesw`): GUIDED | FREE. Free hides the strip and leaves the bench alone; Guided brings the strip back exactly where you left off. Landing anything before Begin flips the toggle to Free (the old "offer" arrow is gone — the toggle is the way back). Beside it THE CHIP (`.pos`): "Ch 3 · The shells · 3 of 4", live in both modes ("Not started" on a fresh load; the invitation's caption reads "Continue" when there is progress). Clicking the chip opens THE CONTENTS PANEL (`#panelContents`, the panel family, 640 wide): every chapter with its steps as one-line rows (the step's first sentence), a "done" mark on badged chapters, the current step outlined in amber; steps you have reached are live and the rest wait dimmed; chapters 7–12 read "not written yet". Clicking a live step jumps there — bench cleared, that step's atoms handed again, guided mode on — and `state.reached` (the furthest point) is kept so nothing closes behind you. Verified in the pane: load with saved progress → chip + Free; Guided → strip at ch 3 step 3; chip → contents (9 of 21 steps live); jump to ch 1 step 2 → strip + atoms + panel closed; Free → strip gone, chip still reads the position. Console clean. Tags: css 35, app 38, guide 5.

His first read of the contents: "weird" — he was at chapter 3 step 3 and nothing past helium was live, and there was no restart. Cause: his save predated `reached`, so it loaded as chapter 1 step 1. `load()` now counts the saved position as reached. And a START OVER button in the contents header (chapter 1, badges cleared, guided mode on); the finish screen's "start over" uses the same `restart()`. Verified: old-style save → 9 steps live incl. all three of chapter 3's reached; start over → ch 1 step 1, no badges, guided. Tags: css 36, app 39, guide 6.

Then, after start over: "some blue circle with a blue dot in the middle... I can't move it or touch it" — the close-up drawing of a single hydrogen beside the real handed hydrogen. Rule now: the close-up appears ONLY on steps that wait for a build, as the target picture (nine steps lost theirs); everywhere else the real atoms are the picture. Tag guide 7.

Start over then redefined by him: a testing tool for now ("what's the point when you can click chapter one?"), and it must mean NO progress — "as if I haven't seen anything yet." `restart()` now wipes the state, turns the badges off, closes the guide and returns to the untouched invitation ("Begin", Free, chip "Not started"). Verified. Rail order flipped to FREE | GUIDED and the chip sits 4px off the pair, the same gap as the rail tabs (`.modes`). Tags: css 37, app 40, guide 8.

Chapter 3 step 3: the Li→F target drawing sat over the carbon and nitrogen on the bench — "the colored stylized atoms... can't be conflicting with the atoms that are on the actual bench." The drawing now lives INSIDE the strip, under the text and above the arrow (260×130, `.closeup` is a block in `#guide`, display:none when the step has no target), so it can never sit over the bench. Verified in the pane. Tags: css 38, guide 9.

**THE CONTENTS IS A TABLE OF CONTENTS, NOT A GATE (his "do it" on Claude's five points).** Progress is localStorage only (key `snap-guide`, this browser, this machine — no server, no cookie, no account), so: (1) every step is a click away for anyone; reached steps are highlighted (amber dot, full ink) instead of the rest being disabled; (2) a jump lands every atom the chapter handed BEFORE that step, then the step's own, so the bench matches the text (chapter 1 step 2 arrives with both hydrogens — the old jump had this bug); (3) badges stay honest — only finishing a chapter badges it, however you got there; (4) every step has an address, `#ch3-2` = chapter 3 step 2: a link opens straight into the guide at that step (invitation skipped, toggle on Guided), the address follows you as you go, hashchange jumps too, start over clears it; (5) start over is unchanged in meaning. Also: handed atoms now land BELOW the guide strip (`handY()` reads the strip's bottom), not under it. Verified: link entry, 0 disabled rows, jump to salt 3 of 4 → six atoms, console clean. Tags: css 39, app 42, guide 10.

This is most of step 7's map, arrived early on his ask; the Map rail tab stays a placeholder until he says what else it holds.

## 2026-09-04 — Claude (Fable 5.1) — Rail, mode note, the way back to the guide

James, still flying: the rail's controls collided with the Elastic Space dashboard + speaker icons in the corner — the cluster (heat / mute / clear) now sits 72px in from them. The running mode note ("Free · you made water") is OFF (`.mode{display:none}`, the element stays; he will decide later whether notes live there). And the real gap: land one atom before Begin and the invitation was gone with no way back into guided mode. Now `hideInvite()` (when not triggered by Begin) calls `guide.offer()`: the small arrow stays at the top of the bench captioned "guide" — or "continue the guide" when there is saved progress — and a click starts it; the invitation's own caption reads "Continue" when progress exists. The pause button is hidden in that minimized state. Also swept the last mock-voice lines out of app.js ("Bring it something with an open hand", "Break something", "The table is right there", "step 7 of the build"). Verified in the pane: land H → offer shown → click → guide runs; mode hidden; cluster clear. Tags: css 34, app 37, guide 4.

## 2026-09-04 — Claude (Fable 5.1) — James's first flight of the guide: four bench changes

His reads of step 5 while flying it, each built on the spot:

1. Chapter 3's close-up drawing sat on top of the eight-atom row ("some circles in the middle are getting in the way") — steps 1 and 2 of the shells chapter show no close-up now; the row on the bench is the picture.
2. The ledger ("2 · 1 of 8 · gives 1") was "too obscure" — it must keep saying which shell is the outer one. Now two lines: `SHELL 1 FULL · SHELL 2: 1 OF 8` then `GIVES 1` (hydrogen `SHELL 1: 1 OF 2 / WANTS 1`; sodium `SHELLS 1–2 FULL · SHELL 3: 1 OF 8`; ions `CHARGE +1`). Canvas letterSpacing 0.1em replaces the space-joined tracking so the longer line stays narrow enough.
3. His question about boron (why not want five) turned into a brief: show the charge balance, nucleus against electrons, "relatively simple numeric... not too obtrusive... let's whittle." First cut: a third line under the ledger, `8 P⁺  8 E⁻  0` — protons in warm, electrons in the electron blue, the net dim when zero and bright in its sign's colour when the atom is an ion. The X-ray hold now adds only the configuration (its old p/e/q line moved into this). AWAITING his read: three lines under a name may be too much; the neutral line could vanish until a charge appears; the panel header and hover card are the next places for it. Also noted to him: the boron tile says "gives 3", the bench says "WANTS 3" (shares three); the bench is truer, his word changes the tile.
4. "Shouldn't it say H2O somewhere?" — MOLECULE NAME TAGS: while a completed component's formula matches one of the 24 panel molecules, its name (Instrument Serif) and formula (mono) sit above the molecule (below it if there is no room), fading in over 0.6 s from the snap; the tag goes when a bond breaks. `drawMoleculeTags` in engine.js, keyed by the engine's formulaKey of each panel entry (all 24 keys distinct). His words: "this is just a framework and actually it's pretty goddamn cool" — the 24 first, more later.

Cache tags: engine 11, guide 3.

## 2026-09-04 — Claude (Fable 5.1) — Step 5: guided mode, chapters 1–6

James picked step 5 ("don't get too cutesy... a little brevity and a calm, pleasant approach") and, rather than read a plan he could not picture, said "take a rip." Built solo:

`guide.js` (new): the twelve chapters as data (1–6 written, 7–12 titled stubs) plus the guide. Each step has a line of text, a close-up drawing (`show`, drawn by SNAP_MINI on a 360×240 canvas in the middle of the bench), the tiles to light, the atoms to hand (landed apart via landSet, below the close-up; the shells chapter lands its eight in a row via the new `landRow`, one row when the bench is wide enough past the snap reach, else two), and what wakes the arrow: `made:KEY` (the engine's report), `made:KEY×N`, `refuse` (a full atom turned something away — new `sc.onRefuse` hook in the engine's bounce), `break` (a bond broke with the heat up — new `sc.onBreak` in breakEvent), or nothing. The arrow dims to 38% while it waits and its caption becomes the hint ("bring them together"); when the snap lands it wakes and nudges once. Advancing past a chapter's last step badges the chapter's tiles (`.badge-on`: a tiny drawn atom in the tile's tint, CSS only) and clears the bench for the next chapter. Chapter + step + badges persist in localStorage `snap-guide`; Begin resumes where you left off. A `pause` button folds the strip to the small arrow ("resume") and frees the bench. Past chapter 6 the strip says how far the guide goes and offers "start over". The mode line reads "Guided · 1 of 12 · Hydrogen".

The chapters: 1 hydrogen (one atom, then H₂), 2 helium (push it into a hydrogen; the refusal is the rule), 3 the shells (Li→Ne in a row, hold for the X-ray, then Li + F), 4 water, 5 O₂ then N₂ then the heat dial (wait for a bond to break), 6 salt (one pair, the ionic line, then two more pairs toward a crystal), and a closing line. Register: rule first, brief, calm, no story.

Also this pass: every engine whisper still in the mock's "hands and rings" voice was rewritten into the register (MADE_LINES, the snap/handoff/charge lines, the pulled-apart lines, the full/no-room refusals; "Show-off" is gone). landSet gained a quiet mode and an anchor, and a fix: a single handed atom used to land exactly on top of whatever was already at the anchor (the trial grid had zero spread for n=1) and the overlap ate it — it now walks the grid at 120px.

Verified in the pane by stepping the engine by hand (the pane's rAF is frozen, so the sim was driven with SnapEngine.step): all six chapters end to end at a 1920×1080 bench — every wait woke on the right event, badges landed on all twelve tiles, the finish state rendered; console clean. Cache tags: css 33, app 36, engine 8, guide 2.

AWAITING JAMES'S READ: the strip's look and type size, the close-up drawing, the arrow's wait/wake, the copy of all six chapters, the row landing on his screen, whether a chapter should clear the bench (it does now), and the badge mark on the tiles. Then step 6 (chapters 7–12) and step 7 (the map).

## 2026-09-04 — Claude (Fable 5.1) — Step 4: the element card set

Built solo on James's "move on with the next step" (he was away; the four calls from the plan went Claude's way: opens from the symbol button and from a click on a landed atom; tabs Overview / Shells / Numbers / In the world; the 18 bench elements hand-written, the other 100 generated; the Molecules panel family, 560 wide, one element at a time with ◂ ▸ by atomic number).

`content.js` (new): for H → Ar an Overview paragraph and an In the world paragraph in the §15 register (rule first, count the electrons out loud, no story), three italic one-liners each (they land on Overview, Shells, In the world — Numbers stands alone), and boil / density / found-year. For the other 100 (James, on the first report: "thought you were going to add in data for every element" — the plan said all 118): the lighter form, hand-written for every one of them — an In the world paragraph, one italic line, boil / density / found (boil marked ~ where estimated; no boil or density for Fm onward, where none is measured). Overview = the generated rule-first paragraph, Shells = a generated reading of the ladder, Numbers = the rows that have data (absent, never dashes). A coverage check asserts all 118 have world + line + found.

`#panelElement` (index.html + snap.css `.panel.elem`): header with the symbol in a tinted square, name in Instrument Serif, Z · appetite, prev/next; four mono tabs underlined in the element's tint; the Shells tab draws its own ladder — `shells.js` factored so `buildLadderInto()` / `lightLadder()` are shared with the Shells panel (behaviour there unchanged). Ways in: the tile's symbol button (`openElement`), and on the bench a press-and-release on an atom with no drag and no X-ray hold; the bench's close-panel-on-press now only fires on empty bench so the atom click can resolve. Escape / tab / empty-bench click close it as with every panel. The pinned-card placeholder from the buttonify preview is gone. Verified in the pane at 1280×720: O across all four tabs, Fe (generated path), the H symbol click, the C atom click; console clean. Cache tags: css 32, app 33, shells 3.

AWAITING JAMES'S READ: the panel's look, the paragraphs (18 × 2), the numbers rows, whether the generated 100 read well enough, and whether clicking an atom to open it fights with dragging (6px threshold, same as the tiles).

## 2026-09-04 — Claude (Fable 5.1) — Buttonified tiles, group 1 preview

James's brief ("buttonify" — his word): every tile becomes two buttons. A 1px line in a darker step of the tile's own border tint separates the symbol from the name; clicking the symbol opens the info card, clicking the name lands an atom. His orders for the first pass: style ONLY the first column (H, Li, Na, K, Rb, Cs, Fr) so he can judge the theming before it goes table-wide; the card for now is the existing hover card, pinned open (the step-4 tabbed card set will take its place); a slight fill difference between the halves is fine; no text rewritten.

Built: `--td` (the border tint at alpha 0.72, set beside `--tb` in `tint()`); class `bt` on group-1 tiles; strip state = vertical divider with the symbol half at a 9% tint fill (22% on hover, name half 10% on hover); full-table state = the divider runs horizontally under the big symbol. Pointerdown records the zone; a click without movement on the symbol half calls `pinCard()` (click-away, Esc, or another tile's pointerdown unpins; `.card.pinned` gets pointer events), on the name half lands the atom as before; drag from either half still works. Verified in the pane at 800 and 1100 wide: pin, land, both table states.

**Round two, his read of round one:** the divider came out BRIGHTER than the border (a higher alpha of a saturated tint reads brighter on dark) — now `--td` is the same hue at oklch L 0.40, solid, and a second 1px line `--td2` (L 0.08) sits just to its right as a thin drop shadow. THE GRID: the symbol button is one fixed width per letter count — `--sw1` for one-letter symbols, `--sw2` for two — measured in `fitType()` from the widest symbol of each count at the current symbol size + 6px each side; the name size now fits beside the WIDER button (every tile treated as buttonified for the fit, so the whole table's type came down a hair). Hover: the outer border no longer goes white on `bt` tiles; each half lights on its own with an inset 1px ring in the tint at alpha 0.78 (`--th`) and a slightly stronger fill. Hovering anywhere on the tile still shows the card after the pause; hovering a different tile while one is pinned unpins it first.

**Round three:** one width for every symbol button, the two-letter width (`--sw2`), at his ask — to see it. `--sw1` still computed, unused.

His read of round three: "nice. looks great." → the `bt` class is on ALL 118 (the two f-block placeholders stay plain), and the symbol click now opens the step-4 element panel below.

## 2026-09-04 — Claude (Fable 5.1) — Snap! becomes a world (step 10, part one)

James, before the night's wrap ("I want to see Snap in the Elastic Space In Progress Worlds"): the code moved out of `tmp/snap/` into `src/worlds/snap/` as a DRAFT world — dashboard-control wired, the shared sound control attached to the engine's chords (a master gain now sits behind every tone so the volume slider works; the rail's mute button and the speaker drive the same state; no autoplay attempt), `world.json`, this changelog, a CLAUDE.md, `reimagine.md` moved here (a pointer left behind). Admin panel row in In progress worlds with the "unwired" note. No drift exits and no registry entry until ship. `tmp/snap/index.html` is now a redirect to the world so his bookmark keeps working; the Design package stays at `tmp/snap/design/`.

THE VALENCE LAB IS NOT RETIRED. His call after the Scope idea (reimagine.md §15 item 3): the lab's coherence scope should come out onto the Snap bench over a molecule — the real electron cloud and the shells at separate brightness, nucleus and proton cloud — "it'd be a shame not to use it at all." Both worlds stay in the list.

Where things stand: steps 1–3 built and read (Shells panel awaiting his read after the centring pass); NEXT step 4 (the data + the per-element tabbed card set) on his go; also on the list with their own gos: bench editing (box-select, Delete, Ctrl-C/V/D, right-click menu), the Scope on the bench, the pullback.
